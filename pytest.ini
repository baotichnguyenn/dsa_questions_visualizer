[pytest]
; importlib mode lets every problem folder keep a file called test_algorithm.py
; without the basenames colliding when pytest is run from this root folder.
addopts = --import-mode=importlib
testpaths = .
norecursedirs = harness _template .pytest_cache __pycache__
